# Kubernetes manifests

Plain YAML for the API (`api.yaml`).

For a values-driven install with the same image, use `helm/authinject-rag/` instead of maintaining a second copy of environment variables.
